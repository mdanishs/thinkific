
const ExtraData = ({ title, description }) => {
  return (
    <div className="d-flex flex-column align-items-center">
      <div>{title}</div>
      <div>{description}</div>
    </div>
  )
}

export default ExtraData;